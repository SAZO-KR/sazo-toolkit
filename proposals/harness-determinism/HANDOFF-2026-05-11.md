# Handoff — Harness Determinism Workstream

**작성**: 2026-05-11
**대상 세션**: PR #29 merge 후 진행할 새 Claude Code 세션

## 현재 상태 한 줄

PR #27 (verdict footer), #30 (ci_passed_at), #29 (workflow CLI) 까지 진행. #29 머지 대기 (현재 round 18, 모든 review 답변 완료, 회귀 zero).

## 완료된 work

| PR | 내용 | 상태 |
|---|---|---|
| #27 | Plan 01 — verdict footer + aggregation + nonce | ✅ MERGED (9563b8a) |
| #30 | Plan 04 — ci_passed_at invalidation + GH#34692 fallback | ✅ MERGED (d339eaf) |
| #31 | sleep-guard → awake CLI 재설계 (별 작업) | ✅ MERGED (48b0652) |
| #29 | Plan 02 — workflow CLI (sazo-workflow) | ⏳ Round 18 답변 완료, merge 대기 |

## Wave 1 후속 — 다음 plan 후보

`proposals/harness-determinism/00-INDEX.md` 참조. 의존 그래프:

```
PR #29 merge 후 ─┬─→ Plan 13 (control flow extensions)
                 │   - Stage S0 spike: ✅ 완료 (proposals/harness-determinism/spike-stop-hook.md, SessionEnd hook 사용)
                 │   - mark_approval_complete, /skip vocabulary, post-session-end-metrics,
                 │     post-task-output-audit (warn-only), mark_skip_with_check (auto-skip block)
                 │   - 1.5주 estimate
                 │
                 ├─→ Plan 05 (circuit breaker / degraded mode)
                 │   - 이미 v2 revision 됨 but BLOCK 잔존 (8 issue) — re-revision 필요
                 │   - PR #29 의 audit_log JSON Lines 활용 가능
                 │   - 0.5주 estimate
                 │
                 ├─→ Plan 06 (Phase 1 default ON — narrow hooks)
                 │   - PR #29의 sazo-workflow stats 가 dogfood 데이터 분석 enabler
                 │   - 0.3주
                 │
                 └─→ Plan 12 (Phase 2 default ON — workflow-state-machine)
                     - 의존: Plan 06 + dogfood 30일
                     - 후순위
```

독립 (PR #29만 필요):
- Plan 07 — Test edits counter (warn only, 0.5주)
- Plan 08 — Bot review GitHub API label gate (1주)
- Plan 09 — Skip streak hard escalation (0.3주)
- Plan 10 — Dangerous command sandbox (0.3주)
- Plan 11 — Approval nonce idle TTL (0.3주)

모든 plan은 plan-critic BLOCK 받음 — revision 필요 (각 1-2시간).

## 우선순위 추천 (혼자 진행 시)

1. **PR #29 merge** (현재)
2. **Plan 13** revision + 구현 (integrator 채택 enabler, 1.5주)
   - S0 spike 결과 반영해 Stage A 재명세 (post-stop → post-session-end-metrics, timestamp 필드 minimum subset)
3. **Plan 02 follow-up** — workflow CLI 셀프리뷰 follow-up:
   - A2 helper unused (state_set_dict_value production 미호출) — Plan 04에서 발견됨
   - Plan 04 follow-up도 함께
4. **Plan 06 Phase 1 default ON** — dogfood 시작 (0.3주, narrow hooks만)
5. **dogfood 2주** + sazo-workflow stats 모니터링
6. 이후 Plan 07-11 우선순위 재평가

## 작업 패턴 (이번 세션에서 검증된)

### 서브에이전트 위임 전략 (메인 컨텍스트 절약)

- **plan revision + 구현**: `general-purpose` subagent
- **자동 리뷰 사이클** (Codex/Gemini): `general-purpose` subagent (skill: `automated-code-review-cycle`)
- **/review (code-reviewer + architect-advisor)**: `general-purpose` subagent (skill: `review`)
- **rebase + conflict 해결**: `general-purpose` subagent
- **plan-critic 검증**: `plan-critic` subagent 직접

### TDD + 회귀 zero 강제

- 모든 fix는 TDD: 테스트 먼저 (RED) → 구현 (GREEN)
- 매 commit 후 `CLAUDE.md` ai-harness CI 명령 모두 PASS 확인
- 회귀 발견 시 즉시 멈춤

### Codex/Gemini 사이클 안전 가드

- max 5 round, wall-clock 30분
- bot login 정확 매칭 (substring 금지)
- 답변은 commit/push **이후** (commit_id anchor)
- 동의 = 멘션 없음, decline = `@<reviewer>` 멘션

### 중요한 발견 (Codex 대량 정정 누적)

- shell command parsing 우회 패턴들 (git -C, --diff-filter, pathspec form, opaque chain, -am, evasion)
- glob/find -name vs literal sid 차이
- jq path interpolation 위생
- bash 3.2 호환 (${!var} 금지, case statement)
- BSD vs GNU date/stat/find 차이

## 미해결 / 보류

- **Codex/Gemini 무한 spiral 가능성** — 지난 PR #30 22 round 정정 같은 패턴. 매 round 새 corner case. 결정 기준 필요 — "더 이상 수정하지 않고 follow-up issue로" 시점
- **integrator 측 채택** — `/Users/hakun.lee/work/integrator/docs/harness-determinism-plan-2026-05-10.md` Phase 0~3 단계. PR #29 merge 후 Phase 1 dogfood 진입 가능
- **Plan 02 cmd_audit awk pre-filter** — Codex round 14 reviewer가 declined 항목 (ts-first-key 가정). 향후 audit log schema 변경 시 재고려
- **A2 helper (state_set_dict_value)** — PR #30 셀프리뷰 follow-up에서 정의됨 but production 미호출. Plan 13 진행 시 채택

## 새 세션 컨텍스트

### 활성 worktree

```
/Users/hakun.lee/work/sazo-toolkit/.worktrees/feat/ai-harness-workflow-cli/  (PR #29)
/Users/hakun.lee/work/sazo-toolkit/.worktrees/feat/ai-harness-verdict-footer/  (PR #27 merged, 정리 가능)
/Users/hakun.lee/work/sazo-toolkit/.worktrees/feat/ai-harness-ci-passed-invalidate/  (PR #30 merged, 정리 가능)
```

PR #29 merge 후 worktree 정리: `clean-worktrees` skill 활용.

### 주요 파일

- `proposals/harness-determinism/00-INDEX.md` — 전체 plan 목록 + 우선순위
- `proposals/harness-determinism/01-verdict-footer-aggregation.md` (✅ PR #27)
- `proposals/harness-determinism/02-workflow-cli.md` (⏳ PR #29)
- `proposals/harness-determinism/04-ci-passed-invalidate.md` (✅ PR #30)
- `proposals/harness-determinism/13-control-flow-extensions.md` (다음 후보)
- `proposals/harness-determinism/spike-stop-hook.md` (Stage S0 PASS)
- `~/.claude/CLAUDE.md` — ai-harness MANAGED BLOCK (워크플로우 정책)

### 환경

- 메인 worktree: `/Users/hakun.lee/work/sazo-toolkit` (main 브랜치)
- branch: 매 plan 별 새 branch (`git worktree add` via isolate skill)
- bot login: `chatgpt-codex-connector[bot]`, `gemini-code-assist[bot]`
- Codex quota: ~5h reset, Gemini: ~24h
