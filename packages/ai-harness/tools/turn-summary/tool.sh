# tool.sh - turn-summary tool metadata
# Sourced by the root installer to discover available tools.
# Convention: tools/*/tool.sh must exist and define these variables.

TOOL_NAME="turn-summary"
TOOL_DESC="Stop hook: summarize each working turn and surface decisions"
TOOL_VERSION="1.0.0"
TOOL_PLATFORM="any"          # "any" for cross-platform, or specific OS
TOOL_REQUIRES_SUDO="no"      # "yes" | "no" | "optional"
